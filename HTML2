<!DOCTYPE html>
<html>
<head>
    <title>Karnataka Temple Crowd Management</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        .temple-card {
            transition: transform 0.2s;
        }
        .temple-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Karnataka Temple Crowd Management System</h1>
        <p class="text-center text-muted mb-5">Real-time crowd monitoring for temples across Karnataka</p>
        
        <div class="row">
            {% for temple in temples %}
            <div class="col-md-4 mb-4">
                <div class="card temple-card h-100 shadow-sm">
                    <div class="card-body text-center">
                        <h5 class="card-title">{{ temple.name }}</h5>
                        <p class="card-text text-muted">{{ temple.location }}</p>
                        <a href="/temple/{{ temple.id }}" class="btn btn-primary">Monitor Crowd</a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</body>
</html>
